import LiveSearch from './LiveSearch'

export default {
  title: 'LiveSearch',
  component: LiveSearch,
}

const Template = args => <LiveSearch> Read more </LiveSearch>

export const Primary = Template.bind({})
